services:
  backend:
    build: ./backend
    #PROD ONLY
    # command: >
    #   sh -c "echo 'Starting Daphne ASGI server' && python -m daphne -b 0.0.0.0 -p 8000 config.asgi:application"
    #DEV ONLY
    command: sh -c "python manage.py runserver 0.0.0.0:8000"
    volumes:
      - ./backend:/app
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"
    restart: always

  frontend:
    build: ./frontend
    # Run vite via node executable to avoid wrapper/script execution issues on mounted volumes
    command: ["sh", "-c", "node /app/node_modules/vite/bin/vite.js --host"]
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules
    ports:
      - "8001:8001"
    env_file:
      - ./backend/.env
    restart: always

  proxy:
    image: nginx:stable-alpine
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./backend/cert.pem:/etc/ssl/certs/cert.pem:ro
      - ./backend/key.pem:/etc/ssl/private/key.pem:ro
    ports:
      - "8080:80"
      - "443:443"
    depends_on:
      - backend
      - frontend
    restart: always

volumes:
  frontend_node_modules:
  
